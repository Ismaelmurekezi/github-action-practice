name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main    

jobs:
  build:
    runs-on: ubuntu-

    steps:
      - name: Checkout code
        uses: actions/

      - name: Install dependencies
        run: npm install


